# 인스타툰 발행

노션 `포스팅 글 모음`에서 평소처럼 글을 작성합니다.

- `type`: Post, `status`: Public, `tags`: 인스타툰
- `slug`, `date`, `summary`, `thumbnail`을 지정합니다.
- 본문 최상위에 이미지 블록을 컷 순서대로 연속 배치합니다. 빈 문단이 있어도 컷 사이의 읽기 흐름을 끊지 않습니다.
- 각 이미지의 **캡션**에 컷 제목과 대사를 씁니다. 블로그에서 이미지 설명 및 ‘그림 속 이야기, 글로 읽기’에 사용합니다.
- 소개나 마무리, 서비스 링크는 이미지 묶음 앞뒤에 일반 노션 블록으로 작성합니다.

이미지를 같은 폭의 세로 웹툰으로 이어 보여줍니다. 컷 번호와 넘김 버튼 없이 아래로 스크롤해서 읽습니다. 업로드 이미지와 외부 HTTPS 이미지 모두 사용할 수 있습니다. 인스타툰 글의 단일 이미지도 같은 웹툰 폭을 사용하며, 일반 글은 기존 노션 렌더링을 유지합니다. 향후 노션에 직접 이미지를 업로드하면 코드 수정이나 이미지 재배포가 필요하지 않습니다. 기존 Notion 캐시 주기(통상 약 5~7분)에 따라 글 목록·본문·사이트맵·RSS가 갱신됩니다.

## 이번 발행

- `/post/yeondang-toon-1`: 운세를 읽었는데, 그래서 뭘 하지? (8컷)
- `/post/yeondang-toon-2`: 친구한테 “이거 해봐”만 보내던 개발자 (8컷)

출처는 `istp-developer/output/instatoon/yeondang-01/lettered-v2`와 `yeondang-02`의 최종 레터링 PNG입니다. 1편 첫 컷은 사용자가 선택한 긴 내레이션 버전입니다. `public/instatoon/` 파일은 원본을 그대로 복사했고, 배포 시 이미지 최적화 기능으로 반응형 WebP를 제공합니다. 이 폴더의 파일을 이동하거나 삭제하면 노션 외부 이미지 링크도 함께 수정해야 합니다.
